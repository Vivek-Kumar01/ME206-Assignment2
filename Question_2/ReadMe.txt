This sub folder contains:
1. screenshot of the output and pdf containing the code
2. graph of r (in mi) vs time (in sec) and theta (in rad) vs time (in sec) with respective fit curves
3. the python file that contains the code (Question_2.py)

To run the python file,
1. Make sure a code editor (like VS code or PyCharm) with python interpreter (install from python.org) is installed.
2. Through terminal install the imported libraries (numpy, matplotlib)
3. Run the python file to see the output.

Comments has been added for explanation and clarifications.